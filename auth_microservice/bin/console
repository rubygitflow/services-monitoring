#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../config/environment'

puts "Loading #{ENV['RACK_ENV']} environment (Roda #{Roda::RodaVersion})"

require 'irb'
IRB.start(__FILE__)
